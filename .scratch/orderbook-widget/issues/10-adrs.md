# Adrs

Type: task
Status: open
Blocked by: 03, 04, 05, 06, 07, 08, 16, 17

## Question

Write the ADRs under `docs/adr/` (per `domain-modeling` ADR format) for the decisions that are hard to reverse and surprising without context: snapshot-native engine (why no delta/sequence logic), integer-tick prices, engine outside React with sampled presentation, Canvas 2D over WebGL/DOM with the Worker deferral criterion, size-delta field instead of OFI, synthetic-labelled benchmarks. Keep each to the format; link the resolving ticket.
