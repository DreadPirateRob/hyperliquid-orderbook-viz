# Implementation

Type: task
Status: open
Blocked by: 09, 12

## Question

Umbrella over the tickets from Tickets. Each slice is worked with the `implement` skill: TDD at the seams agreed in the spec, typecheck and single test files continuously, full suite once at the end, `code-review` on completion, commit on `master`. Visual slices are never delegated to AFK agents; the user signs the parity gate. **No code is copied from `attempt/react-v1`.**
