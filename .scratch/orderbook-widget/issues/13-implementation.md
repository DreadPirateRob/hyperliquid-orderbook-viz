# Implementation

Type: task
Status: open
Blocked by: 09, 12

## Question

Build the widget to the spec. This slice is larger than one session: on claiming, split it into ordered sub-tickets (feed adapter + state machine; engine diff + lifecycle; metrics; renderer + animation; controls + HUD; tests + benchmarks) and work them one per session.
